// Appended questions and source-derived controls for the installed policy.
// Routing outcomes are captured by probe.py, not recreated in JavaScript.
function policyQuestions(){return [
 {id:'path',group:'Who does what',title:'What is Path, and what can it change?',
  answer:['Path is the plan-editing role. The Overseer calls its action replan. It asks a model for the smallest useful change to the project’s goal/TODO tree, then code validates and applies the change. It does not write the implementation files or launch a build.','This is a different authority boundary from Chat and Brainstorm: their TODO suggestions are proposals you adopt. A successful Path response is applied directly, without that adoption step. A request such as “Break this down” is recognized as planning; a passed verification can also lead the model to choose Path.'],
  detail:'The table below lists all eight supported operations. Protection is operation-specific: completed TODO rows cannot be revised or removed, but renaming a subgoal with completed rows and reordering a busy subgoal are not blocked by those checks.',widget:'path',
  sources:[ref('agents/path.py',11,118),O(446,467)],related:['chat-brainstorm','overseer-matrix','orchestrator-rules','bart']},
 {id:'chat-brainstorm',group:'Who does what',title:'What is the difference between Chat and Brainstorm?',
  answer:['Chat answers what you said. Brainstorm develops options, questions, or candidate TODOs. They are different prompts and response schemas; they can use the same configured interface model. Both appear as Bart in the conversation.','The distinction does not reliably look like a different UI. Brainstorm’s questions and choices are converted into ordinary text with options underneath, which you answer by typing. Its TODO cards become adoptable rows. Chat can also propose up to three TODOs, so “it suggested a TODO” does not tell you which role answered.'],
  detail:'Example: “How does Bart work?” normally goes to Chat. “What are my options?” is forced to Brainstorm. “Break this down” is forced to Path. The pending-build-question branch can intercept all three before normal routing.',widget:'roles',
  sources:[ref('agents/chat.py',25,97),ref('brainstorm.py',431,448),ref('agents/brainstorm.py',21,36),ref('agents/replies.py',13,84),O(53,76),O(214,286)],related:['path','overseer-matrix','bart-loop','bart-context']},
 {id:'overseer-matrix',group:'Who does what',title:'What are all the fixed and model-selected Overseer options?',
  answer:['There are six actions: none, chat, brainstorm, replan (Path), build, and verify. Only ten event types enter routing. Three lifecycle event types skip the Overseer model entirely; several others call it but force a fixed action.','The model can choose the next action for ordinary Bart wording (Chat, Brainstorm, or Path) and for passed verification (None, Chat, Brainstorm, or Path). Recognized options/planning phrases constrain Bart routing first. Invalid output or a routing exception uses the fixed fallback.'],
  detail:'“Deterministic” describes the control rule given its inputs. A model or the environment can still supply those inputs. Even a locked action may retain a model-written explanation, target IDs, and normalized context updates. This table is specifically the Overseer; its model-call count excludes responders, discovery, acceptance generation, and verification.',widget:'policy',
  sources:[ref('agents/policy.py',20,128),ref('agents/overseer.py',9,169),O(146,209),O(469,488),C(182,191)],related:['orchestrator-rules','chat-brainstorm','path','rules','bart-loop']},
 {id:'orchestrator-rules',group:'Who does what',title:'What are all the deterministic behaviors of the orchestrator?',
  answer:['The orchestrator is Python control flow. It records events, assembles state, asks the Overseer when policy permits, applies context updates, dispatches roles, and manages build/verification transitions. It is reconstructed for a turn or event; the event log, context ledger, and repair counts persist on disk.','Every branch in the inventory below is fixed code. That does not make the whole run predictable: role selection can depend on model output; Chat classifies uncertainty; Path proposes edits; acceptance, discovery, builders, and verifiers can involve models or changing runtime state.'],
  detail:'Scope: the behavior of agents/orchestrator.py in installed 0.20.4 with the default role implementations. The last group links to fixed rules owned by other modules, so they are not mistaken for orchestrator decisions.',widget:'orchestrator',
  sources:[O(1,50),O(79,142),O(146,286),O(288,347),O(349,488),O(492,666),O(678,720)],related:['overseer-matrix','path','verification','fast-rules','bart-loop']}
];}

function policyTable(headers,rows){return '<div class="policy-table-wrap" tabindex="0" role="region" aria-label="Comparison table"><table class="policy-table"><thead><tr>'+headers.map(h=>'<th scope="col">'+esc(h)+'</th>').join('')+'</tr></thead><tbody>'+rows.map(r=>'<tr>'+r.map((c,i)=>i?'<td>'+esc(c)+'</td>':'<th scope="row">'+esc(c)+'</th>').join('')+'</tr>').join('')+'</tbody></table></div>'}

function roleComparison(){return '<h3>Same conversation, different responsibilities</h3>'+policyTable(['Role','Model output','What changes in your workspace'],[
 ['Chat','say + up to 3 todos + needs; optional resolution and contextUpdates','Prose and adoptable TODO proposals. Can record learned context and request human help or directory discovery.'],
 ['Brainstorm','One card: questions, focus, goals, todos, offer, or none; plus prose/contextUpdates','Cards normalize into text or TODO proposals. The focused goal-page prompt asks for flat TODOs, not new goals/subgoals or an offer-first exchange.'],
 ['Path','say + changes (at most 12 operations)','Validated plan edits are applied directly to stored goals/TODOs. No code implementation or automatic build in this role.']
 ])+'<details class="policy-detail"><summary>Context and presentation differences</summary><p>Chat receives the supplied conversation, thin project context, selected goal/subgoal titles, known context, and optional discovery results. Brainstorm wraps the legacy card generator, using its bounded conversation window (last 40 turns, up to 2,000 characters each) plus focus and any question being handed over. Path receives bounded assembled project state, including the plan and current run, plus the conversation and known context.</p><p>Chat’s “two to five sentences” and Brainstorm’s “reasonable options” are prompt requests. They are not guarantees enforced by counting sentences or evaluating option quality. The three-TODO Chat cap, supported card grammar, and Path operation validation are code checks.</p>'+guideSources([ref('agents/chat.py',58,97),ref('agents/brainstorm.py',21,36),ref('agents/replies.py',31,71),O(446,467)])+'</details>';}

function pathOperations(){return '<h3>All supported plan operations</h3>'+policyTable(['Operation','Effect','Enforced restriction'],[
 ['keep_plan','Leave the plan alone.','Empty changes also keeps the plan.'],
 ['add_subgoal','Create a subgoal, optionally with TODOs.','Existing parent and nonempty title required.'],
 ['revise_subgoal','Rename a subgoal.','Rejects any building, queued, or asking row. Completed rows do not prevent renaming.'],
 ['reorder_subgoal','Move a subgoal before a sibling, or to the end.','Rejects self-reference and a before-target with a different parent. No busy/completed-row guard here.'],
 ['replace_subgoal','Replace title and TODO list.','Rejects any busy or completed row.'],
 ['add_todos','Append new TODOs at depth 0.','Nonempty text; up to 20 rows in one operation. Existing busy rows do not prevent adding.'],
 ['revise_todos','Change existing TODO text and optional acceptance.','Rejects missing, busy, or completed rows. Clears old acceptance first.'],
 ['remove_obsolete_todo','Remove one TODO.','Rejects missing, busy, or completed rows.']
 ])+'<details class="policy-detail"><summary>What makes a patch valid?</summary><p>The orchestrator snapshots the goal tree before asking Path. Applying the response takes a session lock and rejects it if the tree changed in the meantime. Code validates the full patch on a copy, sanitizes it, and saves once. Unsupported operations, absent targets, blank required text, or invalid row collections reject the patch. Path accepts at most 12 operations; operations that carry TODO lists accept at most 20 items each.</p><p>The prompt says “Do not change running or completed work.” The operation-specific checks above are the actual enforcement. Renaming or reordering is not protected identically to rewriting or removing a completed TODO.</p></details><details class="policy-detail"><summary>Illustrative Path JSON — not a captured model response</summary><pre>'+esc(JSON.stringify({say:'Add a step to capture expected outcomes.',changes:[{op:'add_todos',subgoalId:'g002',todos:[{text:'Add an expected-output field'}]}]},null,2))+'</pre><p>This would add the TODO after validation. It would not build the field.</p></details>';}

const overseerRows=[
 ['bart.message: ordinary wording','Yes','Chat / Brainstorm / Path','Chat'],
 ['bart.message: recognized options request','Yes; action locked','Brainstorm','Brainstorm'],
 ['bart.message: recognized planning request','Yes; action locked','Path','Path'],
 ['plan.requested','Yes; action locked','Path','Path'],
 ['chat.needs_human','Yes; action locked','Brainstorm','Brainstorm'],
 ['chat.needs_discovery','Yes; action locked','Chat → discovery branch','Chat'],
 ['todo.build_requested','No','Build','Build'],
 ['build.completed','No','Verify','Verify'],
 ['build.failed','Yes; action locked','None','None + failure context'],
 ['build.question','Yes; action locked','None*','None*'],
 ['verify.passed','Yes','None / Chat / Brainstorm / Path','None + success context'],
 ['verify.failed: repairs used < 2','No','Build → repair','Build → repair'],
 ['verify.failed: repairs used ≥ 2','No','Chat → fixed escalation*','Chat → fixed escalation*'],
 ['Every other event type','No','No routing','None if route() is called directly']
];
let selectedPolicyCase='bart',selectedPolicyAction='build';
function policyWidget(kind){
 if(kind==='queue')return queueWidget();
 if(kind==='path')return pathOperations();
 if(kind==='roles')return roleComparison();
 if(kind==='orchestrator')return orchestratorInventory();
 if(kind!=='policy')return '';
 return '<div class="policy-lab"><h3>Try a routing conflict</h3><p>Choose an event and a hypothetical model answer. The outcome comes from the real installed routing function, run offline with a stub model.</p><div class="policy-controls"><div><label for="policy-event">Incoming event</label><select id="policy-event">'+E.routing_cases.map(c=>'<option value="'+c.id+'" '+(c.id===selectedPolicyCase?'selected':'')+'>'+esc(c.label)+'</option>').join('')+'</select></div><div><label for="policy-action">Suppose the model returns</label><select id="policy-action">'+['none','chat','brainstorm','replan','build','verify','invalid','exception'].map(a=>'<option value="'+a+'" '+(a===selectedPolicyAction?'selected':'')+'>'+esc(a==='replan'?'replan (Path)':a==='invalid'?'invalid action':a==='exception'?'throws an exception':a)+'</option>').join('')+'</select></div></div><div id="policy-result" aria-live="polite"></div></div><h3>Complete event table</h3>'+policyTable(['Event reaching Overseer','Overseer model called?','Permitted action after guards','Fallback'],overseerRows)+
 '<p class="qa-detail">* None means no ordinary role dispatch. build.question has a special handler that still calls Chat to classify the builder’s question. After repair exhaustion, Chat without a carried conversation invokes a fixed escalation template, with no Chat model call.</p>'+policyDetails();
}
function renderPolicyResult(){
 const c=E.routing_cases.find(c=>c.id===selectedPolicyCase),o=c.outcomes[selectedPolicyAction],d=o.decision;
 const actual=c.id==='smalltalk'?'chat':d.action;
 let note=!o.model_calls?'The model is not called for this event. Its hypothetical answer is never read.':selectedPolicyAction==='invalid'||selectedPolicyAction==='exception'?'The output is unusable, so the installed code returns the fixed fallback.':d.action!==selectedPolicyAction?'The model suggestion is overridden by a hard routing guard.':'This model action is permitted for the selected event.';
 if(c.id==='smalltalk')note+=' The orchestrator then forces recognized smalltalk to Chat and the current subgoal, even if the Overseer returned another conversation role.';
 if(c.id==='question')note+=' None still enters the special builder-question classifier; it does not mean nothing happens.';
 if(c.id==='escalate')note+=' The ordinary build-finished path carries no conversation here, so Chat dispatch emits the fixed escalation question instead of calling a Chat model.';
 const label=a=>a==='replan'?'Path':a.charAt(0).toUpperCase()+a.slice(1);
 $('policy-result').innerHTML='<div class="policy-outcome"><div><span class="small">Overseer model calls</span><strong>'+o.model_calls+'</strong></div><span aria-hidden="true">→</span><div><span class="small">Overseer action</span><strong>'+label(d.action)+'</strong></div><span aria-hidden="true">→</span><div><span class="small">Orchestrator dispatch</span><strong>'+(c.id==='notes'?'Ignored':c.id==='question'?'Question handler':c.id==='escalate'?'Escalation':label(actual))+'</strong></div></div><p>'+esc(note)+'</p>'+(c.event.payload.text?'<p class="small">Sample message: “'+esc(c.event.payload.text)+'”</p>':'')+'<p class="small">Fallback: '+esc(c.fallback.action)+' · '+esc(c.fallback.reason)+'</p><details class="policy-detail"><summary>Inspect the captured decision JSON</summary><pre>'+esc(JSON.stringify({event:c.event,state:{attempts:{g002:c.attempts}},hypotheticalModelAction:selectedPolicyAction,overseerModelCalls:o.model_calls,returnedDecision:d},null,2))+'</pre></details>';
}
function policyDetails(){return '<details class="policy-detail"><summary>What can still vary when an action is locked?</summary><p>A valid model response can supply a reason (capped at 600 characters), targetSubgoalId, targetTodoId, and contextUpdates. The orchestrator accepts a different subgoal target only if it exists, and never retargets build-requested, build-completed, or verify-failed events. Recognized smalltalk also forces the current subgoal. Context updates are normalized and appended to the deterministic fallback updates; they are not free-form code execution.</p><p>The nine accepted context kinds are new_fact, user_preference, project_constraint, decision, discovered_dependency, todo_status, run_result, artifact, and verification_result. Model updates are capped at 12. These are durable context-ledger entries; a todo_status context entry does not itself rewrite the actual TODO row.</p><p>The model is asked to record only explicit preferences and learned facts. Code checks the update schema, not whether a claimed fact is true. A fixed route therefore does not guarantee a fixed explanation or reliable retained context.</p>'+guideSources([ref('agents/overseer.py',132,169),C(65,101),C(182,191),O(146,188)])+'</details>'+ 
 '<details class="policy-detail"><summary>Exactly which wording forces Brainstorm or Path?</summary><p>Brainstorm is tested first. Recognized patterns include “brainstorm…”, “what are my/the/some options/choices/alternatives”, requests to give/show/list options or ideas, “other/different/alternative ways/approaches/options”, weighing options or trade-offs, “which way/approach/option should/would…”, and “help me choose/decide/pick”.</p><p>Path patterns include “plan this/it out”, “break this/it down/into”, “what are the steps”, “lay out the steps/rows/work/plan”, “help me plan”, “plan this/the project/our next steps”, “let’s plan”, and drafting/writing/proposing a plan or rows for something. The exact regex defines the boundary; arbitrary synonyms remain a model decision.</p><p>If both patterns match, Brainstorm wins. If neither matches, the fallback is Chat, but a valid model can choose Brainstorm or Path. This is why the same intent phrased differently need not follow an identical routing branch.</p>'+guideSources([ref('agents/policy.py',90,128)])+'</details>'+ 
 '<details class="policy-detail"><summary>What is ignored, and what bypasses this table?</summary><p>Only the ten named meaningful event types route. Notes edits, TODO creation/editing/toggling/removal, goal creation, saved chat, preview operations, build/verify started, discovery finished, context updated, role replied, path planned, repair requested, and verification escalated do not trigger Overseer routing merely by being recorded. Other modules can still do work or call models in response to those operations.</p><p>Recording an event with emit() does not itself call handle(). Repair escalation records chat.needs_human but does not immediately dispatch Brainstorm. A pending build question also intercepts non-smalltalk Bart messages before handle(): it calls Chat directly to decide resume/cancel/wait. That is the branch behind the repeated reply in your screenshot.</p><p>The orchestrator’s overseer_calls counter increments for routing attempts, including the branches that make zero Overseer model calls. Do not interpret that counter as model usage.</p>'+guideSources([ref('agents/policy.py',20,84),O(105,118),O(146,163),O(229,286),O(469,488)])+'</details>';}

const orchestrationGroups=[
 {title:'1. Entry points, durable state, and event policy',rules:[
  ['Enable switch','The new agent path is enabled unless HC_AGENTS, stripped of whitespace, equals "0". Module entry points bypass it when disabled.'],
  ['State lifetime','Each orchestrator instance is temporary. Events, context, and per-subgoal repair counts live on disk. Missing or unreadable agent state falls back to an empty attempts map; updates use a session lock and atomic write.'],
  ['Record versus route','emit() records an event. handle() separately checks the ten-type meaningful-event whitelist. Ordinary edit notifications are recorded after the edit and do not route.']
 ],sources:[O(1,50),O(105,155),O(688,720)]},
 {title:'2. Before and after the Overseer decision',rules:[
  ['Success context first','For verify.passed, fixed success/TODO-status context is applied before calling the Overseer. These are context records, not a second build.'],
  ['Routing input and trace','Assemble current project/event state, invoke the routing function, record overseer.routed, apply its context updates, and save a brief step/result trace. Invalid context updates raising ValueError are skipped here.'],
  ['Smalltalk override','Recognized greetings/thanks force Chat and the current subgoal after routing. The model may still have been called.'],
  ['Retargeting','A model-selected different subgoal must exist. Retargeting is blocked for build requested, build completed, and verification failed.'],
  ['Dispatch','Six actions map to no ordinary dispatch, Chat, Brainstorm, Path, Build, or Verify. build.question always takes its dedicated handler even with action none. build.failed attempts a routine failure publication before stopping.']
 ],sources:[O(146,209),ref('agents/presentation.py',1,18)]},
 {title:'3. Bart messages and pending questions',rules:[
  ['Message entry','Read the last user turn; gather project context and selected titles; record bart.message. Try a pending question before ordinary routing unless the message is recognized smalltalk.'],
  ['Pending conversation question','Route normally with the pending question attached. Mark it answered only for resume/cancel if the same pending event is still current.'],
  ['Pending build question','Call Chat directly as a classifier. If it still reports a need, or does not return resume/cancel, return the old question. This fixed branch can swallow an unrelated new question.'],
  ['Resume, cancel, or error','Resume calls runtime.answer with the new user text; cancel calls runtime.cancel. Success records human.answered and optionally build.cancelled. Resume errors reinstate the pending question and return an error.']
 ],sources:[O(214,286)]},
 {title:'4. Questions raised by the builder',rules:[
  ['Choose a row','Take the first stored TODO that is asking and has a question; no such row means no handler result. Ask Chat to classify that question.'],
  ['Human need','A successful human_preference classification creates a pending build question for that row and publishes the question.'],
  ['Discovery fallback','Every other classifier result falls through to directory discovery, including failed/empty classification. If the same build question was already discovered since the last build request, fail the row instead of repeating discovery.'],
  ['Automatic continuation','Discover, record the discovery, then resume the builder with the result. Discovery/resume failure marks the row failed, clears its question, and records build.failed.']
 ],sources:[O(288,347)]},
 {title:'5. Chat follow-ups and presentation',rules:[
  ['No transcript','Chat dispatch without a conversation calls the fixed escalation routine instead of a Chat model.'],
  ['Discovery request','For chat.needs_discovery with no discovered text, run directory discovery, record a dependency context entry, and pass the result to Chat.'],
  ['Normalize the answer','Learn normalized context updates, cap/sanitize prose, strip internal IDs unless debug detail was requested, and deduplicate proposed TODO text against existing rows and earlier proposals. Smalltalk removes needs and proposals.'],
  ['Human uncertainty','If the model reports human_preference and this carried conversation has not asked yet, record the question and route to Brainstorm. Combine successful replies. The asked_human flag prevents repeating that handoff within the carried flow.'],
  ['Environment uncertainty','If the model reports environment and discovered text is still empty, route to discovery and Chat again. This check depends on a nonempty discovery result; there is no separate numeric retry limit in this branch.'],
  ['Return','Bad responder output returns an error. An empty reply list gets a fixed “Bart had nothing to add.” text. Record chat.replied and return any resolution.']
 ],sources:[O(349,423),ref('agents/chat.py',81,124),ref('agents/presentation.py',1,18)]},
 {title:'6. Brainstorm and Path',rules:[
  ['Brainstorm handoff','Pass transcript, thin context, selected focus, known context, and any handed-over question. Learn valid returned context and record brainstorm.replied.'],
  ['Background questions','For background Brainstorm questions/focus cards with text replies, save the last text as a pending conversation question. Recording it does not automatically route again.'],
  ['Path mutation','Snapshot the goal tree, ask Path with assembled state plus routing reason, then apply its validated patch. A stale or invalid patch returns an error. Applied changes record the current-path decision; record path.planned. See Path for all eight operation guards.']
 ],sources:[O(425,467),ref('agents/path.py',46,118)]},
 {title:'7. Initial builds and preview failures',rules:[
  ['Rows and lane','Retain string row IDs. Unless quick is explicitly supplied, look up all selected rows plus descendants and call build.prefer_quick. The lane classifier itself lives in build.py.'],
  ['Initial build','Record/request routing; ensure saved acceptance criteria before starting. Acceptance failure prevents launch. Reset the subgoal repair counter, invoke runtime.build, and record build.started on success.'],
  ['Launch result','A failed request records build.cancelled. Queued/joined results close the request’s lifecycle telemetry. Backend runtime/build code implements queueing and joining.'],
  ['Preview failure entry','Select done rows covered by the saved run’s acceptance. With none, return an error. Otherwise record preview-startup evidence (with bounded log tail) as verify.failed and feed it through the same repair policy.']
 ],sources:[O(492,572)]},
 {title:'8. Repair budget and escalation',rules:[
  ['Choose repair target','Use a decision target TODO if present, otherwise the first failed row, otherwise the first row. Increment the counter for the subgoal.'],
  ['Repair request','Compose a repair note from the reason, all row IDs, and evidence. Record build.repair_requested, call runtime.repair, and record build.started on success.'],
  ['Two repair attempts','verify.failed with fewer than two repairs used selects Build; after two it selects Chat. The normal no-transcript branch emits a fixed readable escalation question. Neither that routing choice nor the escalation wording needs a model.'],
  ['Escalation records','Publish the templated work update; record chat.needs_human with resume=build and all affected rows; then record verify.escalated with the raw reason. Reset the repair counter. The latest-event UI bug can replace the readable question with that reason.']
 ],sources:[O(469,488),O(574,600),ref('agents/overseer.py',82,92)]},
 {title:'9. Completion and verification',rules:[
  ['Translate builder state','idle → build.completed; failed → build.failed; waiting → build.question; all other endings → build.cancelled. Record the event, then handle it. A successful Build result means a repair started.'],
  ['Run verification','Record verify.started, call the verifier, persist its evidence, and interpret verdict.passed as a boolean. Record verify.passed or verify.failed; append run-result and optional artifact context. The verifier determines the checks and verdict.'],
  ['Success follow-up','On pass, reset the counter and route with a synthetic “explain the verified result” conversation plus background context. The Overseer can choose None, Chat, Brainstorm, or Path. A background Brainstorm question can be published; a regular returned Chat explanation is not automatically published by this branch.'],
  ['Failure follow-up','On failure, route the recorded verdict through repair/escalation. The module-level build_finished wrapper catches orchestration exceptions so they do not propagate back into the builder.']
 ],sources:[O(602,666),O(710,720)]},
 {title:'10. Fixed behaviors owned by other modules',rules:[
  ['Build implementation','Nested-row grouping, quick/full classification, process launch/resume, backend queue/join behavior, clean-exit TODO status, estimates, and builder protocol parsing live in build/runtime code.'],
  ['Verification implementation','Acceptance criteria, supported artifact checks, browser inspection, preview startup conditions, and optional verdict adjudication live in acceptance/verifier/artifact/preview modules.'],
  ['UI and publication','Which event wins the status display, whether each row shows a reply box, and how saved Bart replies render are UI rules. communication.publish also suppresses routine verified/repair/failed messages and deduplicates publication. These are not choices made by an Overseer model.']
 ],links:['nested','fast-rules','concurrency','terminals','time-estimates','token-estimates','done','criteria','verification','error-prompts'],sources:[ref('agents/communication.py',32,55)]}
];
function orchestratorInventory(){return '<div class="inventory-intro"><h3>Complete control-flow inventory</h3><p>Expand a boundary to see the rule, its variable inputs, and the source. “Fixed” does not mean “correct”: the pending-question trap and raw-error escalation are fixed branches too.</p></div>'+orchestrationGroups.map(g=>'<details class="policy-detail inventory-group"><summary>'+esc(g.title)+'</summary><dl>'+g.rules.map(([name,body])=>'<div><dt>'+esc(name)+'</dt><dd>'+esc(body)+'</dd></div>').join('')+'</dl>'+(g.links?'<div class="guide-links">'+g.links.map(id=>qButton(id)).join('')+'</div>':'')+guideSources(g.sources)+'</details>').join('');}
document.addEventListener('change',e=>{if(e.target.id==='policy-event'){selectedPolicyCase=e.target.value;renderPolicyResult()}if(e.target.id==='policy-action'){selectedPolicyAction=e.target.value;renderPolicyResult()}});

function queueQuestions(){return [
 {id:'queue-status',group:'Context & follow-up work',title:'Does the queue work? Are new TODOs or code changes stashed?',
  answer:['In the standard headless workspace, saving more TODOs while the same subgoal is busy does not automatically queue another batch. They remain saved, unstarted rows. The Build controls and handler block dispatch while that subgoal is building, checking, or fixing; you press Build again once it is available.','The backend has two separate mechanisms: it can join new rows to a live headless build, or it can queue a prompt for the connected Claude session when HC_BUILD_MODE=session is explicitly selected. The older headless later.json queue is only drained for compatibility; current submissions do not write new batches to it.','If by “stashed” you mean git stash: Engelbart does not automatically stash each TODO’s edits or isolate each TODO in its own worktree. The builder writes into the project’s working directory. A replacement process or conversation sees those files as they stand. Saved TODO state, saved Claude transcript, and code on disk are three different things.'],
  detail:'What was tested: the installed backend accepted a join with a stubbed live process, preserved its conversation/lane, and composed its real row-update message. The connected-session queue stored then drained a prompt and changed rows from queued to building in temporary state. This does not establish that a real connected session’s hooks currently pick up work, or that Claude completes it correctly.',widget:'queue',
  sources:[A(686,715),ref('web/goal/components/todos.js',8,14),ref('web/goal/store.js',121,138),B(396,496),B(1867,1892),B(1993,2046),B(2072,2179),B(1398,1472)],related:['same-subgoal-notes','concurrency','different-subgoal','new-notes']},
 {id:'same-subgoal-notes',group:'Context & follow-up work',title:'New batch, same subgoal: new Claude process, same context, or fresh Notes?',
  answer:['A process and a Claude conversation are different identities. A running headless process receives no new stdin from Engelbart. If the backend joins more TODOs, it stops that process and starts a replacement with --resume and the same Claude session ID. It does not normally open a visible terminal window.','That join keeps the existing run and lane; it returns before composing a fresh full/quick prompt. Its message names the added rows, any saved acceptance, the remaining work, and a request to update the estimate. It does not attach the newly saved Notes. Resuming a conversation preserves its earlier context; it does not synchronize that context with the Notes tab.','After the old run finishes, a later full build starts a new conversation with fresh Notes. A later quick build omits Notes and may resume a retained quick conversation (up to eight uses, with an existing transcript). An old full conversation is not automatically reused as the quick conversation. The standard Build handler saves pending Notes before dispatch, but quick mode still leaves them out of its prompt.'],
  detail:'If the live run ends before it can accept a join, the backend falls through to ordinary start logic. A live restart-check phase is stopped first. These exceptions mean “same subgoal” alone is not enough to predict the session ID. The standard UI normally prevents reaching the mid-build join at all.',widget:'queue',
  sources:[A(686,715),B(1578,1615),B(1738,1757),B(1993,2179),B(2263,2281),B(2325,2352)],related:['queue-status','new-notes','later-build','fast-rules','terminals']}
];}

let selectedQueueCase='busy';
const queueScenarios=[
 {id:'busy',label:'Standard UI: same subgoal still busy',headline:'Saved rows; no new build request',steps:['Batch A is building, checking, or fixing.','You save Notes and two new TODOs.','Build is blocked by the UI guard.','The new rows remain unstarted until you build them later.'],notes:'The running builder receives no automatic Notes update or row-update message. This is persistence, not automatic scheduling.',process:'No replacement process or new Claude conversation.',payload:null},
 {id:'joined',label:'Backend: join a live headless build',headline:'Replacement process, same conversation',steps:['A backend build request reaches an existing live row-building run.','The backend adds rows to that run and calls redirect().','The old process stops; its reader starts claude -p --resume with the row-update message.','Existing transcript and working directory remain; the run keeps its current lane.'],notes:'New Notes are not attached as a snapshot. Saved acceptance may carry individual constraints, but this message does not refresh the Notes tab. A full-lane request cannot force a fresh full prompt when this branch joins first.',process:'Another process invocation; the same Claude session ID. No automatic terminal window.',payload:'join'},
 {id:'full',label:'After completion: later full build',headline:'New conversation, fresh Notes snapshot',steps:['The previous run has ended.','The standard Build handler flushes TODO edits and pending Notes saves.','The backend composes a full prompt from current saved state.','It launches a new Claude conversation in the same project directory.'],notes:'Saved Notes are included at prompt-composition time. Later edits still do not refresh that running prompt.',process:'New claude -p process and new session ID.',payload:'full'},
 {id:'quick',label:'After completion: later quick build',headline:'New invocation; Notes omitted',steps:['The previous run has ended.','The request qualifies for quick mode, or quick is selected.','The backend composes the smaller quick prompt.','It resumes an eligible retained quick conversation, otherwise starts a new one.'],notes:'Notes are not attached, even though the UI saved them first. A retained transcript can contain earlier information; that is not a guarantee that it contains the newest Notes.',process:'New claude -p invocation; same retained quick session if eligible, otherwise a new ID.',payload:'quick'},
 {id:'session',label:'Alternate mode: connected-session queue',headline:'Stored prompt delivered by a future hook',steps:['HC_BUILD_MODE=session changes the backend transport.','Build stores the already-composed prompt in queue.json; selected rows become queued.','A Stop or UserPromptSubmit hook drains all queued messages and marks rows building.','The connected Claude session receives the combined messages. No separate builder process starts.'],notes:'A full queued prompt contains the Notes from enqueue time. Notes edited after enqueue are not recomposed when it is delivered. Quick queued prompts omit Notes. No hook firing means no pickup; the queue is cleared before Claude acknowledges receipt.',process:'Existing connected session; no additional claude -p process.',payload:'session'}
];
function queueWidget(){return '<div class="policy-lab"><h3>Follow your new batch</h3><p>The selected path determines both session reuse and Notes inclusion. These controls illustrate installed behavior; they submit no work.</p><div class="queue-case"><label for="queue-scenario">When and how are the new TODOs submitted?</label><select id="queue-scenario">'+queueScenarios.map(s=>'<option value="'+s.id+'" '+(s.id===selectedQueueCase?'selected':'')+'>'+esc(s.label)+'</option>').join('')+'</select></div><div id="queue-result" aria-live="polite">'+queueResult()+'</div></div><details class="policy-detail"><summary>What does “queue works” mean in this audit?</summary><p>Nine assertions passed against the real installed join/queue functions using temporary goal state. The join preserved the existing conversation and lane, included the new rows, omitted the saved Notes, and created no queued item. The connected-session queue stored/drained a prompt, changed queued rows to building, preserved its enqueue-time snapshot, and returned nothing on a second delivery.</p><p>The live process was stubbed, so the join test does not verify termination, Claude --resume, or eventual completion. No actual Claude session or hook was exercised. The source shows hook-dependent delivery and no acknowledgement/requeue protocol at the queue boundary; this is not evidence of reliable end-to-end scheduling.</p><p>Code edits are already in the shared working directory. Queueing stores instructions, not a per-TODO code snapshot or a git stash.</p></details>';}
function queueResult(){const s=queueScenarios.find(s=>s.id===selectedQueueCase);let payload='',caption='';if(s.payload==='join'){payload=E.queue_probe.joined_message;caption='Real joined_message() output · temporary fixture, redirect stubbed'}if(s.payload==='full'){payload=E.later_full_prompt;caption='Real full prompt · synthetic later batch'}if(s.payload==='quick'){payload=E.later_quick_prompt;caption='Real quick prompt · synthetic later batch'}if(s.payload==='session'){payload=E.queue_probe.session_delivered_message;caption='Real deliver(..., "Stop") output · temporary queue, no actual hook'}return '<h4>'+esc(s.headline)+'</h4><ol class="queue-steps">'+s.steps.map(x=>'<li>'+esc(x)+'</li>').join('')+'</ol><p><strong>Notes:</strong> '+esc(s.notes)+'</p><p><strong>Process/session:</strong> '+esc(s.process)+'</p>'+(payload?'<details class="policy-detail"><summary>Inspect the message for this path</summary><p class="small">'+esc(caption)+'</p><pre>'+esc(payload)+'</pre></details>':'');}
document.addEventListener('change',e=>{if(e.target.id==='queue-scenario'){selectedQueueCase=e.target.value;$('queue-result').innerHTML=queueResult()}});
