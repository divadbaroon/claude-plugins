[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "human-compact"
version = "0.19.11"
description = "Session-scoped goal workspaces and persistent context for Claude Code."
readme = "README.md"
requires-python = ">=3.9"
authors = [{ name = "David Baroon" }]
license = { text = "MIT" }

[project.scripts]
hc-backup = "human_compact.cli:main"
hc = "human_compact.cli:hc_main"
bart = "human_compact.cli:bart_main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
human_compact = ["assets/**/*", "assets/plugin/.claude-plugin/*", "assets/bart-skill/*", "trajectory/web/*", "trajectory/web/static/*"]
