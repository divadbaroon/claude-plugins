// Fill these in to have the directory connect on load. Leave them empty and
// the app will ask for them once and remember the answer in localStorage.
//
// The anon (public) key is the right key here. It is public by design and is
// meant to ship in a page; the service-role key never belongs in a browser.
window.BERKELEY_CONFIG = {
  url: '',
  anonKey: '',
};
